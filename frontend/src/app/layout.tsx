import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CareerPath AI | Your AI-Powered Career Mentor",
  description: "Get personalized career guidance, skill gap analysis, and week-by-week roadmaps powered by Amazon Bedrock AI.",
  keywords: ["career", "AI", "mentor", "skills", "roadmap", "job search"],
  openGraph: {
    title: "CareerPath AI",
    description: "Your AI-Powered Career Mentor",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-gray-950 text-gray-100`}
      >
        {children}
      </body>
    </html>
  );
}
